[build-system]
requires = ["setuptools >= 64"]
build-backend = "setuptools.build_meta"

[project]
name = "remake"
description = "Smart remake tool"
readme = "README.md"
license = { file = "LICENCE" }
authors = [
    { name = "Mark Muetzelfeldt", email = "mark.muetzelfeldt@reading.ac.uk" },
]
maintainers = [
    { name = "Mark Muetzelfeldt", email = "mark.muetzelfeldt@reading.ac.uk" },
]
requires-python = ">=3.10"
dependencies = [
    "loguru",
    "networkx",
]
dynamic = ["version"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Natural Language :: English",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: Apache Software License",
    "Topic :: Scientific/Engineering",
]

[project.urls]
Homepage = "https://github.com/markmuetz/remake"
Documentation = "https://markmuetz.github.io/remake"
"Bug Tracker" = "https://github.com/markmuetz/remake/issues"

[project.optional-dependencies]
debug = [
    "ipdb",
]
s3 = ["boto3"]
dask = [
    "distributed>=2024.8.0",
]
# Everything examples/ needs beyond the (deliberately minimal) core deps:
# `pip install "remake[examples]"` / `uv add "remake[examples]"`.
examples = [
    "xarray>=2024.7.0",
    "netcdf4>=1.7.2",
    "h5netcdf",
    "zarr<3",
    "dask",
    "pyyaml",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "hypothesis",
    "xarray>=2024.7.0",
    "netcdf4>=1.7.2",
    "h5netcdf>=1.8.1",
    "h5py",
    "zarr<3",
    "dask",
    "pyyaml>=6.0.3",
    "distributed>=2024.8.0",
]
docs = [
    "mkdocs",
    "mkdocs-material",
    "mkdocstrings[python]",
]

[project.scripts]
remake = "remake.remake_cmd:remake_cmd"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = { attr = "remake.version.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["remake"]
branch = true

[tool.coverage.report]
show_missing = true
fail_under = 85
